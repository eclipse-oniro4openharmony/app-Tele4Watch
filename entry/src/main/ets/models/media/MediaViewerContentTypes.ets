export interface MediaViewerContentSize {
  width: number;
  height: number;
}

/**
 * Describes the zoom and pan transform applied to image viewer content.
 */
export interface MediaViewerImageTransform {
  scale: number;
  offsetX: number;
  offsetY: number;
}
